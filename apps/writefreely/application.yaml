apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: writefreely
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Kagayama0208/homelab.git
    targetRevision: HEAD
    path: apps/writefreely
  destination:
    server: https://kubernetes.default.svc
    namespace: writefreely
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true